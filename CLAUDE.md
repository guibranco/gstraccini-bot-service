## Agent skills

### Issue tracker

Issues live in GitHub Issues (guibranco/gstraccini-bot-service), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root (created lazily as decisions are recorded). See `docs/agents/domain.md`.
